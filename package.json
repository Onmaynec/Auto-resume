{
  "name": "auto-resume",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "check": "node --check app.js && node --check js/config.js && node --check js/data.js && node --check js/projects.js && node --check js/render.js && node --check js/resume.js && node --check js/utils.js && node --check api/github.js",
    "test": "node --test",
    "verify": "npm run check && npm test"
  }
}
